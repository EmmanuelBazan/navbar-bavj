import styled from 'styled-components'

export const PortafolioContainer = styled.div`
    background-color: coral;
    width: "100%";
    height: 100vh;//${({_currentHeight}) => _currentHeight}px;
`;